/**
 * Module Dependencies
 */
var express = require('express'),
    http    = require('http'),
    path    = require('path');

var app = module.exports = express();

















/*
http.createServer(function(req, res){
	var parts           = req.url.split("/");
		jsonCallback    = parts[1];
		idlabel         = parts[2];
		cmpid           = parts[3];
	//var jsonresponse = clservice.init("./web/fw/w/cl", "./test/fw/web/fw/w/cl", 1, req, res);  // generate the respone from the service..
	res.writeHead(200, {'Content-Type':'text/html'});
	res.write("Hello World!")
	res.end();
})
http.listen(3000,"http://localhost:3000/");*/
//sys.puts("Server Running at http://localhost:3000/");